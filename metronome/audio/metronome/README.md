# Metronome PCM Assets

Add accent and normal click samples here.

Expected filenames:

- `accent-click.wav`
- `normal-click.wav`

Both should be mono PCM WAV files (44.1 kHz) trimmed for sample-accurate playback. Until these files are present the metronome will fall back to procedural clicks and unit tests depending on the assets will skip.
